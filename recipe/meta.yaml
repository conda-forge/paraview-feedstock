{% set version = "5.6.2" %}
{% set maj_min = ".".join(version.split(".")[:2]) %}

package:
  name: paraview
  version: {{ version }}

source:
  fn: ParaView-v{{ version }}.tar.gz
  url: http://paraview.org/files/v{{ maj_min }}/ParaView-v{{ version }}.tar.xz
  sha256: 1f3710b77c58a46891808dbe23dc59a1259d9c6b7bb123aaaeaa6ddf2be882ea
  patches:
    - vth_header_clamp.patch

build:
  number: 3
  # only enough time on circleci
  skip: true  # [not linux]

requirements:
  build:
    - cmake
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - eigen
  host:
    - python
    - libglu
    - hdf5
    - libpng
    - jpeg
    - libtiff
    - libxml2
    - zlib
    - freetype
    - twisted
    - six
    - libprotobuf
    - lz4-c
    - zope.interface
    - expat
    - libogg
    - libtheora
    - libnetcdf
    - pugixml
    - mesalib
    - glew
    - double-conversion
  run:
    - python
    - libglu
    - hdf5
    - libpng
    - jpeg
    - libtiff
    - libxml2
    - zlib
    - freetype
    - twisted
    - six
    - libprotobuf
    - lz4-c
    - zope.interface
    - expat
    - libogg
    - libtheora
    - libnetcdf
    - pugixml
    - mesalib
    - glew
    - double-conversion
    - numpy

test:
  imports:
    - paraview
    - paraview.annotation
    - paraview.calculator
    - paraview.collaboration
    - paraview.coprocessing
    - paraview.cpexport
    - paraview.cpstate
    - paraview.lookuptable
    - paraview.numeric
    - paraview.numpy_support
    - paraview.python_view
    - paraview.cpstate
    - paraview.servermanager
    - paraview.simple
    - paraview.util

  commands:
    - pvpython --version 2>&1 | grep paraview

about:
   home: http://www.paraview.org
   license: BSD 3-Clause
   license_file: Copyright.txt
   summary: ParaView is an open-source, multi-platform data analysis and visualization application based on Visualization Toolkit (VTK).

extra:
  recipe-maintainers:
    - bgruening
    - jschueller
    - beckermr
